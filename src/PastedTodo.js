import React from 'react';
import PropTypes from 'prop-types';

const PastedTodo = (props) => {
  return (
      <div>
        showing 중
      </div>
  );
};

PastedTodo.propTypes = {
  
};

export default PastedTodo;